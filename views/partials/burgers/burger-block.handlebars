<li>
	{{name}}

	<button class="change-eat" data-id="{{id}}" data-newsleep="{{eaten}}">
		{{#if eaten}}Eaten!{{else}}Whole!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
